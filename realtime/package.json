{
	"name": "realtime",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun --env-file ../.env --bun --watch index.ts",
		"build": "bun build ./index.ts --compile --outfile realtime"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@socket.io/bun-engine": "^0.0.2",
		"msgpackr": "^1.11.5",
		"socket.io": "^4.8.1",
		"web": "workspace:*"
	}
}
